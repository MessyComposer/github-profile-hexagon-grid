<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="100%" height="100%" viewBox="0 0 {{width}} 100">
    
    <style>
        image {
            filter: var(--filter);
            x: calc(var(--x) * 25px);
            transition: all .25s;
        }
        image:nth-of-type(2n) {
            y: 42px;
        }
    </style>
    <style>
    </style>
    {{ #transitions }}
    {{{ . }}}
    {{ /transitions }}

    {{ #hexagons }}
    {{{ . }}}
    {{ /hexagons }}
</svg>
